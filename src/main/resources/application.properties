spring.application.name=summarizer-backend

# ===============================
# Database Configuration (Necessary for connecting to Render PostgreSQL)
# ===============================

# CRITICAL: Uses the External Host and includes 'sslmode=require' to fix the
# 'Could not open SSL root certificate file' error when running locally.
spring.datasource.url=jdbc:postgresql://dpg-d41g69fgi27c739guv80-a.singapore-postgres.render.com/summarizer_43jj?sslmode=require

# Render Database Credentials
spring.datasource.username=summarizer_43jj_user
spring.datasource.password=S8cUo1UGtXHlws840qD7xg7hFbWh7tlK

# Database Driver
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate/JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# HikariCP SSL Mode (matches the URL requirement)
spring.datasource.hikari.ssl-mode=require

# ===============================
# HikariCP Connection Pool (Recommended)
# ===============================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000

# ===============================
# JWT Configuration
# ===============================

# Secret key (should be an Environment Variable in production!)
app.jwtSecret=176MJ80SW3w!Hk:krfoih0A}429=Npk0

# Expiration time in milliseconds (e.g., 86400000 ms = 24 hours)
app.jwtExpirationMs=86400000

# ===============================
# File Upload Configuration
# ===============================
file.upload-dir=uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB